import React from "react";
import { Sun } from "lucide-react";
const Mode = () => {
  return (
    <div>
      <Sun className="w-10 h-10 p-1 border border-primary rounded-full bg-primary text-background" />
    </div>
  );
};

export default Mode;
